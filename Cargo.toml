[workspace]
resolver = "2"

members = [
    "benchmark/pipeline_generator",
    "benchmark/profiler",
    "bic-trip",
    "e2e-tests",
    "mongosql-datastructures",
    "mongosql-c",
    "mongosql",
    "test-utils",
    "schema-builder-library",
    "usererrordisplay-impl",
    "visitgen",
    "visitgen-test",
]

[profile.release]
lto = "fat"
codegen-units = 1
debug = true
