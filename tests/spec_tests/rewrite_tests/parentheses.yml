tests:
  - description: parenthesis are rewritten to be minimal to support order of operations, spaces are put around operators
    query: "SELECT (3+4)*a AS x, 3+(4*a) AS y, ((3+4) + (x + (15+4))) AS z FROM foo"
    result: "SELECT (3 + 4) * a AS x, 3 + 4 * a AS y, 3 + 4 + x + 15 + 4 AS z FROM foo AS foo"
