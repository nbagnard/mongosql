# Evergreen Project Config

# When a task that used to pass starts to fail
# Go through all versions that may have been skipped to detect
# when the task started failing
stepback: true

# Mark a failure as a system/bootstrap failure (purple box) rather then a task
# failure by default.
# Actual testing tasks are marked with `type: test`
command_type: system

# If any of the pre tasks fail, the task will be marked as a failure.
pre_error_fails_task: true

# Do not create patches for changes that only modify the following file types
ignore:
  - "*.md"

# Protect ourselves against rogue test cases, or curl gone wild, that runs forever
exec_timeout_secs: 86400 # 24hrs for TPC-H benchmark

# What to do when evergreen hits the timeout (`post:` tasks are run automatically)
timeout:
  - command: shell.exec
    params:
      script: |
        ls -la

include:
  - filename: evergreen/grpc.yml
  - filename: evergreen/msm.yml
  - filename: evergreen/mongosqltranslate.yml
  - filename: evergreen/suite-tasks.yml

pre:
  - func: "fetch source"
  - func: "create expansions"
  - func: "prepare resources"

post:
  - func: "stop mongo orchestration"

functions:
  "fetch source":
    - command: git.get_project
      params:
        directory: mongosql-rs

  "prepare resources":
    - command: subprocess.exec
      params:
        working_dir: mongosql-rs
        include_expansions_in_env:
          - DRIVERS_TOOLS
        binary: bash
        args:
          - ./evergreen/fetch-drivers-tools.sh

  "bootstrap mongo-orchestration":
    - command: subprocess.exec
      params:
        binary: sh
        args:
          - ${DRIVERS_TOOLS}/.evergreen/run-orchestration.sh
        include_expansions_in_env:
          - DRIVERS_TOOLS
          - TOPOLOGY
          - MONGODB_VERSION
          - MONGODB_BINARIES

  "stop mongo orchestration":
    - command: shell.exec

      params:
        include_expansions_in_env:
          - DRIVERS_TOOLS
          - MONGO_ORCHESTRATION_HOME
          - PROJECT_DIRECTORY
        script: |
          ${PREPARE_SHELL}

          cd ${MONGO_ORCHESTRATION_HOME}
          # source the mongo-orchestration virtualenv if it exists
          if [ -f venv/bin/activate ]; then
            . venv/bin/activate
          elif [ -f venv/Scripts/activate ]; then
            . venv/Scripts/activate
          fi
          mongo-orchestration stop

  "create expansions":
    - command: subprocess.exec
      params:
        working_dir: mongosql-rs
        binary: bash
        env:
          triggered_by_git_tag: ${triggered_by_git_tag}
        args:
          - ./evergreen/create-expansions.sh
        add_expansions_to_env: true
    - command: expansions.update
      params:
        file: mongosql-rs/expansions.yml

  "install rust toolchain":
    - command: shell.exec
      params:
        shell: bash
        script: |
          ${prepare_shell}

          # make sure to use msvc toolchain rather than gnu, which is
          # the default for cygwin
          if [ "Windows_NT" == "$OS" ]; then
              export DEFAULT_HOST_OPTIONS='--default-host x86_64-pc-windows-msvc'
          fi

          # install rustup from scratch
          rm -rf ~/.rustup
          curl https://sh.rustup.rs -sSf | sh -s -- -y --no-modify-path $DEFAULT_HOST_OPTIONS
          # rustup installs into C:\Users\$USER instead of
          # C:\home\$USER, so we symlink both .rustup and .cargo
          if [ "Windows_NT" == "$OS" ]; then
                ln -sf /cygdrive/c/Users/$USER/.rustup/ ~/.rustup
                ln -sf /cygdrive/c/Users/$USER/.cargo/ ~/.cargo
          fi

  "set and check packages version":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          #no-op when not triggered by a tag
          if [[ "${triggered_by_git_tag}" != "" ]]; then
            cargo install cargo-edit
            # we only release one project at a time, so setting the version on all Cargo.toml files is safe
            cargo set-version $release_version
          fi
          cargo install cargo-get

          # check all of the releasable packages in the monorepo to verify that whichever is being released has the correct version
          MONGOSQL_CARGO_PKGS_VERSION=$(cargo get --entry="mongosql/Cargo.toml" package.version)
          TRR_CARGO_PKGS_VERSION=$(cargo get --entry="bic-trip/Cargo.toml" package.version)
          SCHEMA_MANAGER_CARGO_PKGS_VERSION=$(cargo get --entry="mongodb-schema-manager/Cargo.toml" package.version)
          MONGOSQLTRANSLATE_CARGO_PKGS_VERSION=$(cargo get --entry="mongosqltranslate/Cargo.toml" package.version)
          if [[ "${triggered_by_git_tag}" == "" ]]; then
            EXPECTED_RELEASE_VERSION="0.0.0"
          else
            EXPECTED_RELEASE_VERSION="$release_version"
          fi
          if [[ "$MONGOSQL_CARGO_PKGS_VERSION" != "$EXPECTED_RELEASE_VERSION" || "$TRR_CARGO_PKGS_VERSION" != "$EXPECTED_RELEASE_VERSION" || "$SCHEMA_MANAGER_CARGO_PKGS_VERSION" != "$EXPECTED_RELEASE_VERSION" || "$MONGOSQLTRANSLATE_CARGO_PKGS_VERSION" != "$EXPECTED_RELEASE_VERSION" ]]; then
            >&2 echo "Expected version $EXPECTED_RELEASE_VERSION doesn't match for at least one package ; mongosql version $MONGOSQL_CARGO_PKGS_VERSION, trr version $TRR_CARGO_PKGS_VERSION, schema manager version $SCHEMA_MANAGER_CARGO_PKGS_VERSION, mongosqltranslate version $MONGOSQLTRANSLATE_CARGO_PKGS_VERSION"
            exit 1
          fi

  "check clippy":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo clippy --all-targets -- -D warnings

  "check gofmt":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs/go
        script: |
          ${prepare_shell}
          gofmt -s -d .

  "check unused dependencies":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo install cargo-machete
          cargo build --all-features
          set +e
          cargo machete
          RETURN=$?
          set -e
          if [ $RETURN -ne 0 ]; then
            >&2 echo "Unused dependencies found"
            >&2 cargo machete
            exit 1
          fi

  "check goimports":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs/go
        script: |
          ${prepare_shell}
          go get golang.org/x/tools/cmd/goimports
          if [[ $("$GOPATH/bin/goimports" -l .) ]]; then
              echo "some files not formatted according to goimports"
              "$GOPATH/bin/goimports" -d -e .
              exit 1
          else
              echo "files all formatted according to goimports"
              exit 0
          fi

  "check go mod tidy":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs/go
        script: |
          ${prepare_shell}

          # save original files
          cp go.mod go.mod.original
          cp go.sum go.sum.original

          # tidy files and save tidied versions
          go mod tidy
          cp go.mod go.mod.tidied
          cp go.sum go.sum.tidied

          # restore original files
          rm go.mod && cp go.mod.original go.mod
          rm go.sum && cp go.sum.original go.sum

          # fail if tidying changed files
          diff go.mod.original go.mod.tidied || exit 1
          diff go.sum.original go.sum.tidied || exit 1

  "check rustfmt":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          rustfmt --check $(find . -not \( -path \./e2e-tests -prune -o -path \./target -prune \) -name \*.rs)

  "compile go tests":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo build --release --features "mongosql-c/test" --target-dir "test_target"
          cargo build --release

  "compile release":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo build --release

  "download c library":
    - command: s3.get
      params:
        build_variants:
          ["macos", "macos-arm64", "rhel76", "ubuntu1804", "amazon2-arm64"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/test/${version_id}/${build_variant}/libmongosql.a
        local_file: mongosql-rs/libmongosql.a
        bucket: mciuploads
    - command: s3.get
      params:
        build_variants: ["windows"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/test/${version_id}/${build_variant}/mongosql.dll
        local_file: mongosql-rs/go/mongosql/mongosql.dll
        bucket: mciuploads

  "perform release":
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/macos/libmongosql.a
        local_file: mongosql-rs/release/macos/libmongosql.a
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/macos/libmongosql.a
        remote_file: mongosql-rs/macos/${release_version}/libmongosql.a
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/macos-arm64/libmongosql.a
        local_file: mongosql-rs/release/macos-arm64/libmongosql.a
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/macos-arm64/libmongosql.a
        remote_file: mongosql-rs/macos-arm64/${release_version}/libmongosql.a
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/rhel76/libmongosql.a
        local_file: mongosql-rs/release/rhel76/libmongosql.a
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/rhel76/libmongosql.a
        remote_file: mongosql-rs/rhel76/${release_version}/libmongosql.a
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/ubuntu1804/libmongosql.a
        local_file: mongosql-rs/release/ubuntu1804/libmongosql.a
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/ubuntu1804/libmongosql.a
        remote_file: mongosql-rs/ubuntu1804/${release_version}/libmongosql.a
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/amazon2-arm64/libmongosql.a
        local_file: mongosql-rs/release/amazon2-arm64/libmongosql.a
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/amazon2-arm64/libmongosql.a
        remote_file: mongosql-rs/amazon2-arm64/${release_version}/libmongosql.a
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/windows/mongosql.dll
        local_file: mongosql-rs/release/windows/mongosql.dll
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/windows/mongosql.dll
        remote_file: mongosql-rs/windows/${release_version}/mongosql.dll
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream

  "run criterion benchmarks":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo bench --bench translate

  "run criterion profile":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo bench --bench translate -- --profile-time 10

  "run go tests":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs/go
        script: |
          ${prepare_shell}
          export LIBRARY_PATH="$(cd .. && pwd)"
          go test -v ./... | tee go-test-results.txt

  # Just running the spec tests on RHEL 7.6 for now
  # This function should be used to run rust tests that need a running mongod
  # Additional flags can be passed in via the calling task
  # e.g. cargo_test_flags: "-- --ignored"
  "run rust integration tests":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}

          if [[ "${load_data_cmd}" != "" ]]; then
            echo "loading data"
            eval ${load_data_cmd}
          fi

          echo ${description}
          set +e
          cargo test ${cargo_test_flags}
          EXITCODE=$?
          set -e
          exit $EXITCODE

  # This function should be used to run rust tests that do not need a running
  # mongod instance. Additional flags can be passed in via the calling task.
  # e.g. cargo_test_flags: "-- --ignored"
  "run rust tests":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          echo ${description}
          cargo test ${cargo_test_flags}

  "run pipeline generator":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo run --bin pipeline_generator

  "upload c library":
    - command: s3.put
      params:
        build_variants:
          ["macos", "macos-arm64", "rhel76", "ubuntu1804", "amazon2-arm64"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/target/release/libmongosql.a
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/libmongosql.a
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.put
      params:
        build_variants: ["windows"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/target/release/mongosql.dll
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/mongosql.dll
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.put
      params:
        build_variants:
          ["macos", "macos-arm64", "rhel76", "ubuntu1804", "amazon2-arm64"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/test_target/release/libmongosql.a
        remote_file: mongosql-rs/artifacts/test/${version_id}/${build_variant}/libmongosql.a
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.put
      params:
        build_variants: ["windows"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/test_target/release/mongosql.dll
        remote_file: mongosql-rs/artifacts/test/${version_id}/${build_variant}/mongosql.dll
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "upload criterion artifacts":
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/target/criterion/translate_simple/profile/flamegraph.svg
        remote_file: mongosql-rs/artifacts/test/${version_id}/${build_variant}/criterion/flamegraph_translate_simple.svg
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "upload go test results":
    - command: gotest.parse_files
      type: test
      params:
        files:
          - mongosql-rs/go/go-test-results.txt

  "upload transition readiness report executable":
    - command: s3.put
      params:
        build_variants: ["trr-macos", "trr-macos-arm", "trr-ubuntu2004"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/target/release/mongosql-transition-tool
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/mongosql-transition-tool
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
    - command: s3.put
      params:
        build_variants:
          - trr-windows-64
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/target/release/mongosql-transition-tool.exe
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/mongosql-transition-tool.exe
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "sign trr windows":
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-windows-64/mongosql-transition-tool.exe
        local_file: mongosql-rs/mongosql-transition-tool.exe
        bucket: mciuploads
    - command: shell.exec
      type: system
      retry_on_failure: true
      params:
        silent: true
        script: |
          docker login --username ${sql_engines_artifactory_username} --password ${sql_engines_artifactory_auth_token} ${release_tools_container_registry}
    - command: shell.exec
      type: system
      params:
        silent: true
        env:
          GRS_CONFIG_USER1_USERNAME: "${garasign_username}"
          GRS_CONFIG_USER1_PASSWORD: "${garasign_password}"
        working_dir: mongosql-rs
        script: |
          docker run \
            -e GRS_CONFIG_USER1_USERNAME \
            -e GRS_CONFIG_USER1_PASSWORD \
            --rm \
            -v $(pwd):$(pwd) -w $(pwd) \
            ${garasign_jsign_image} \
            /bin/bash -c "jsign -a mongo-authenticode-2021 --replace --tsaurl http://timestamp.digicert.com -d SHA-256 mongosql-transition-tool.exe"
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/mongosql-transition-tool.exe
        remote_file: mongosql-rs/artifacts/${version_id}/trr-windows-64/mongosql-transition-tool-signed.exe
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "sign trr linux":
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-ubuntu2004/mongosql-transition-tool
        local_file: mongosql-rs/mongosql-transition-tool-linux
        bucket: mciuploads
    - command: shell.exec
      type: system
      retry_on_failure: true
      params:
        silent: true
        script: |
          docker login --username ${sql_engines_artifactory_username} --password ${sql_engines_artifactory_auth_token} ${release_tools_container_registry}
    - command: shell.exec
      type: system
      retry_on_failure: true
      params:
        silent: true
        env:
          GRS_CONFIG_USER1_USERNAME: "${garasign_username}"
          GRS_CONFIG_USER1_PASSWORD: "${garasign_password}"
        working_dir: mongosql-rs
        script: |
          docker run \
            -e GRS_CONFIG_USER1_USERNAME \
            -e GRS_CONFIG_USER1_PASSWORD \
            --rm \
            -v $(pwd):$(pwd) -w $(pwd) \
            ${garasign_gpg_image} \
            /bin/bash -c "gpgloader && gpg --yes -v --armor -o mongosql-transition-tool-linux.sig --detach-sign mongosql-transition-tool-linux"
    - command: shell.exec
      type: system
      retry_on_failure: true
      params:
        silent: false
        env:
          GRS_CONFIG_USER1_USERNAME: "${garasign_username}"
          GRS_CONFIG_USER1_PASSWORD: "${garasign_password}"
        working_dir: mongosql-rs
        script: |
          docker run \
            -e GRS_CONFIG_USER1_USERNAME \
            -e GRS_CONFIG_USER1_PASSWORD \
            --rm \
            -v $(pwd):$(pwd) -w $(pwd) \
            ${garasign_gpg_image} \
            /bin/bash -c "gpgloader && gpg --verify mongosql-transition-tool-linux.sig mongosql-transition-tool-linux"
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/mongosql-transition-tool-linux.sig
        remote_file: mongosql-rs/artifacts/${version_id}/trr-ubuntu2004/mongosql-transition-tool-linux.sig
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "sign trr macos":
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/mongosql-transition-tool
        local_file: mongosql-rs/AtlasSQLReadinessReport-macos
        bucket: mciuploads
    - command: subprocess.exec
      params:
        binary: bash
        working_dir: mongosql-rs
        args:
          - "./bic-trip/resources/scripts/sign_macos_zip.sh"
        add_expansions_to_env: true
    - command: s3.put
      params:
        build_variants: ["trr-macos"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/AtlasSQLReadinessReport-macos
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/AtlasSQLReadinessReport-macos-signed
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-macos-signed
    - command: s3.put
      params:
        build_variants: ["trr-macos-arm"]
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/AtlasSQLReadinessReport-macos
        remote_file: mongosql-rs/artifacts/${version_id}/${build_variant}/AtlasSQLReadinessReport-macos-arm-signed
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-macos-arm-signed

  "run TPC-H benchmark":
    - command: shell.exec
      timeout_secs: 86400 # 24 hour timeout for new log entry
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          benchmark/scripts/run_tpch.sh ${pipeline_dir} ${workload_filename} ${dataset_filename} ${EVERGREEN_SF0_01_LIMIT} ${EVERGREEN_SF0_1_LIMIT}
    - command: perf.send
      params:
        file: ./mongosql-rs/processed-tpch-perf-results.json
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/tpch.metrics.tgz
        remote_file: mongosql-rs/artifacts/${version_id}/${pipeline_dir}/${s3_prefix}.tpch.metrics.tgz
        display_name: ${s3_prefix}.tpch.metrics.tgz
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream

  "validate TPC-H benchmark":
    - command: shell.exec
      timeout_secs: 43200 # 12 hour timeout for new log entry
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          benchmark/scripts/run_tpch.sh ${pipeline_dir} ${workload_filename} ${dataset_filename}

  "install heaptrack":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          curl -LO https://mongosql-noexpire.s3.us-east-2.amazonaws.com/mem_usage/heaptrack_build.tar.gz
          tar -xzvf heaptrack_build.tar.gz
          cat <<EOT >> expansions.yml
            export PATH="$PATH:$PWD/heaptrack/bin"
            export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:$PWD/heaptrack/lib"
          EOT
    - command: expansions.update
      params:
        file: mongosql-rs/expansions.yml

  "generate memory usage tasks":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}

          benchmark/scripts/generate_profiler_tasks.sh

          # Print generated json for inspection in case of failure
          cat generated_profiler_tasks.json
    - command: generate.tasks
      params:
        files:
          - generated_profiler_tasks.json

  "perform transition readiness report release":
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-macos/AtlasSQLReadinessReport-macos-signed
        local_file: mongosql-rs/release/trr-macos/AtlasSQLReadinessReport-macos-signed
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-macos/AtlasSQLReadinessReport-macos-signed
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-macos
        bucket: translators-connectors-releases
        permissions: public-read
        skip_existing: false
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-macos
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-macos/AtlasSQLReadinessReport-macos-signed
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-v${release_version}-macos
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-v${release_version}-macos
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-macos-arm/AtlasSQLReadinessReport-macos-arm-signed
        local_file: mongosql-rs/release/trr-macos-arm/AtlasSQLReadinessReport-macos-arm-signed
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-macos-arm/AtlasSQLReadinessReport-macos-arm-signed
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-macos-arm
        bucket: translators-connectors-releases
        permissions: public-read
        skip_existing: false
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-macos-arm
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-macos-arm/AtlasSQLReadinessReport-macos-arm-signed
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-v${release_version}-macos-arm
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-v${release_version}-macos-arm
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-ubuntu2004/mongosql-transition-tool
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-linux
        bucket: translators-connectors-releases
        permissions: public-read
        skip_existing: false
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-linux
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-v${release_version}-linux
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-v${release_version}-linux
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-ubuntu2004/mongosql-transition-tool-linux.sig
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool-linux.sig
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool-linux.sig
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-linux.sig
        bucket: translators-connectors-releases
        permissions: public-read
        skip_existing: false
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-linux.sig
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-ubuntu2004/mongosql-transition-tool-linux.sig
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-v${release_version}-linux.sig
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-v${release_version}-linux.sig
    - command: s3.get
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        remote_file: mongosql-rs/artifacts/${version_id}/trr-windows-64/mongosql-transition-tool.exe
        local_file: mongosql-rs/release/trr-windows-64/mongosql-transition-tool-signed.exe
        bucket: mciuploads
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-windows-64/mongosql-transition-tool-signed.exe
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-win.exe
        bucket: translators-connectors-releases
        permissions: public-read
        skip_existing: false
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-win.exe
    - command: s3.put
      params:
        aws_key: ${release_aws_key}
        aws_secret: ${release_aws_secret}
        local_file: mongosql-rs/release/trr-windows-64/mongosql-transition-tool-signed.exe
        remote_file: transition-readiness-report/AtlasSQLReadinessReport-v${release_version}-win.exe
        bucket: translators-connectors-releases
        permissions: public-read
        content_type: application/octet-stream
        display_name: AtlasSQLReadinessReport-v${release_version}-win.exe

  "run memory usage profiler":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo build --bin profiler --release

          # run_mem_profiler.sh writes three files
          # - memory-usage-results.log: Memory usage metrics to upload to S3
          # - memory-usage-cedar-data.json: Metrics to use in perf.send command
          # - heaptrack.profiler.gz: compressed file of full heaptrack profiling results
          benchmark/scripts/run_mem_profiler.sh "target/release/profiler" ${test_file}
          cat memory-usage-results.log
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/memory-usage-results.log
        remote_file: mongosql-rs/${version_id}/${build_variant}/${test_file_basename}/memory-usage-results.log
        bucket: mciuploads
        permissions: public-read
        content_type: text/plain
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_file: mongosql-rs/heaptrack.profiler.gz
        remote_file: mongosql-rs/${version_id}/${build_variant}/${test_file_basename}/heaptrack.profiler.gz
        bucket: mciuploads
        permissions: public-read
        content_type: application/octet-stream
    - command: perf.send
      params:
        file: ./mongosql-rs/memory-usage-cedar-data.json
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}

          echo "Configured RSS memory limit: ${mem_usage_rss_limit}MB"
          echo "Configured memory leak limit: ${mem_usage_mem_leak_limit}MB"

          rss_peak=$(cat memory-usage-results.log | grep "peak RSS (including heaptrack overhead)" | cut -f2 -d: | tr -d M | tr -d ' ')
          mem_leak=$(cat memory-usage-results.log | grep "total memory leaked" | cut -f2 -d: | tr -d M | tr -d ' ')

          if (( $(bc <<< "$rss_peak > ${mem_usage_rss_limit}") )) || \
             (( $(bc <<< "$mem_leak > ${mem_usage_mem_leak_limit}") )); then
            echo "Memory limit exceeded"
            echo "Peak RSS memory: $rss_peak"
            echo "Memory leaked:   $mem_leak"
            exit 1
          fi

  "run transition readiness report test":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          cargo test --package bic-trip --test log_parse_tests

  "generate SBOM":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}
          echo ">>>> Install SBOM tool..."

          OS=$(uname)
          echo "OS=$OS"
          ARCH="$(uname -m)"
          echo "Arch=$ARCH"
          SBOM_FINAL=${project_folder}.full.cdx.json

          mkdir $SBOM_DIR

          echo "SBOM with vulnerabilities: ./${project_folder}/${project_folder}.cdx.json";
          echo "SBOM with license: $SBOM_VULN";
          echo "Final SBOM with all information: $SBOM_FINAL"

          # Install cargo-cyclonedx
          echo "-- Installing cargo-cyclonedx --"
          cargo install cargo-cyclonedx
          echo "------------------------------------"

          # Install Grype
          echo "-- Downloading Grype --"
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b $SBOM_DIR
          echo "------------------------------------"

          # Install CycloneDX CLI and JQ
          JQ_URL="https://github.com/jqlang/jq/releases/download/jq-1.7.1/jq-linux-arm64"
          echo "-- Downloading JQ $JQ_URL --"
          curl -L -o $SBOM_DIR/jq "$JQ_URL" \
            --silent \
            --fail \
            --max-time 60 \
            --retry 5 \
            --retry-delay 0
          chmod +x ./$SBOM_DIR/jq
          echo "------------------------------------"
          echo "<<<< Done installing SBOM tools"

          echo ">>>> Generate SBOM..."
          echo "--  Generating SBOMs with the licenses information --"
          cargo cyclonedx --target all -v -f json
          echo "------------------------------------"

          echo "-- Generating SBOM with vulnerabilities information --"
          echo "./$SBOM_DIR/grype sbom:./${project_folder}/${project_folder}.cdx.json -o cyclonedx-json > $SBOM_VULN"
          ./$SBOM_DIR/grype sbom:./${project_folder}/${project_folder}.cdx.json -o cyclonedx-json > $SBOM_VULN
          echo "------------------------------------"

          echo "-- Merging the SBOMs with the licenses information and the SBOM with the  vulnerabilities information in $SBOM_FINAL --"

          temp_output="temp_output.json"
          if [[ -f "$temp_output" ]] ; then
              rm "$temp_output"
          fi
          touch $temp_output

          while IFS= read -r line
          do
            if [[ "$line" == *"purl"* ]]; then
              bash_purl=$(echo $line | cut -d '"' -f4)
              command=$(echo "./$SBOM_DIR/jq '.components[] | select(.purl == \"$bash_purl\").licenses' ./${project_folder}/${project_folder}.cdx.json")
              # Add the license information back in the augmented SBOM.
              licenseInfo=$(eval " $command")
              if [[ -z "$licenseInfo" ]]; then
                echo "\"licenses\" : []," >> $temp_output
              else
                echo "\"licenses\" : $licenseInfo," >> $temp_output
              fi
            fi
            echo "$line" >> $temp_output

          done < $SBOM_VULN
          echo "------------------------------------"

          echo "--  Adding the name of the team responsible for each dependency as required by Silk and format the json file --"
          echo "./$SBOM_DIR/jq '.components[].properties += [{\"name\": \"internal:team_responsible\", \"value\": \"Atlas SQL\"}]' $temp_output > $SBOM_FINAL"
          ./$SBOM_DIR/jq '.components[].properties += [{"name": "internal:team_responsible", "value": "Atlas SQL"}]' $temp_output > $SBOM_FINAL
          echo "------------------------------------"

          echo "-- Adding VEX info for vulnerabilities still present in SBOM--"
          IFS=','; for vuln_id in $ALLOW_VULNS; do
            echo "-- Updating SBOM with VEX info for vulnerability with id $vuln_id--"
            export VULN_ID=$vuln_id
            echo "jq --argjson vexinfo "$(<resources/ssdlc/$VULN_ID.analysis.json)" '(.vulnerabilities[] | select(.id == $ENV.VULN_ID)).analysis = ($vexinfo) ' $SBOM_FINAL > output.tmp && mv output.tmp $SBOM_FINAL"
            jq --argjson vexinfo "$(<resources/ssdlc/$VULN_ID.analysis.json)" '(.vulnerabilities[] | select(.id == $ENV.VULN_ID)).analysis = ($vexinfo) ' $SBOM_FINAL > output.tmp && mv output.tmp $SBOM_FINAL
            echo "--------"
          done
          echo "-----------------------------"
          echo "<<<< Done generating SBOM"
    - command: s3.put
      params:
        aws_key: ${aws_key}
        aws_secret: ${aws_secret}
        local_files_include_filter:
          - mongosql-rs/*.cdx.json
        remote_file: mongosql-rs/artifacts/${version_id}/ssdlc/${project_folder}/
        content_type: text/plain
        bucket: mciuploads
        permissions: public-read

  "scan SBOM":
    - command: shell.exec
      type: test
      params:
        shell: bash
        working_dir: mongosql-rs
        script: |
          ${prepare_shell}

          echo ">>>> Scan SBOM for vulnerabilities..."
          if [[ "$ALLOW_VULNS" != "" ]]; then
            echo "Vulnerability ids to ignore : $ALLOW_VULNS"

            echo "-- Generate .grype.yaml specifying vulnerabilities to ignore --"
            GRYPE_CONF_FILE=".grype.yaml"
            touch $GRYPE_CONF_FILE
            echo "ignore:" > $GRYPE_CONF_FILE

            IFS=','; for VULN_ID in $ALLOW_VULNS; do
              echo "Ignoring vulnerability with id $VULN_ID"
              echo "    - vulnerability: $VULN_ID" >> $GRYPE_CONF_FILE
            done
            echo "------------------------------------"
          fi

          echo "-- Scanning dependency for vulnerabilities --"
          ./$SBOM_DIR/grype sbom:./${project_folder}/${project_folder}.cdx.json --fail-on low
          echo "---------------------------------------------"
          echo "<<<< Done scanning SBOM"

tasks:
  - name: clippy
    commands:
      - func: "install rust toolchain"
      - func: "check clippy"

  - name: rustfmt
    commands:
      - func: "install rust toolchain"
      - func: "check rustfmt"

  - name: unused-dependencies
    commands:
      - func: "install rust toolchain"
      - func: "check unused dependencies"

  - name: gofmt
    commands:
      - func: "check gofmt"

  - name: goimports
    commands:
      - func: "check goimports"

  - name: gomodtidy
    commands:
      - func: "check go mod tidy"

  - name: compile
    commands:
      - func: "install rust toolchain"
      - func: "set and check packages version"
      - func: "compile release"

  - name: compile-trr
    commands:
      - func: "install rust toolchain"
      - func: "set and check packages version"
      - func: "compile release"
      - func: "upload transition readiness report executable"

  - name: test-go
    commands:
      - func: "install rust toolchain"
      - func: "compile go tests"
      - func: "upload c library"
      - func: "download c library"
      - func: "run go tests"
      - func: "upload go test results"

  - name: test-pipeline-generate
    commands:
      - func: "install rust toolchain"
      - func: "run pipeline generator"

  - name: test-rust
    commands:
      - func: "install rust toolchain"
      - func: "run rust tests"
        vars:
          description: "run rust unit tests"
          cargo_test_flags: >-
            -- --skip fuzz_test

  - name: test-rust-fuzz
    tags: ["fuzz"]
    commands:
      - func: "install rust toolchain"
      - func: "run rust tests"
        retry_on_failure: true
        vars:
          description: "run rust fuzz tests"
          cargo_test_flags: >-
            fuzz_test

  - name: test-spec-rewrite-and-type-constraints
    commands:
      - func: "install rust toolchain"
      - func: "run rust tests"
        vars:
          description: "run rewrite and type constraint spec tests"
          cargo_test_flags: >-
            -- --ignored

  - name: trr-release
    git_tag_only: true
    depends_on:
      - name: compile-trr
        variant: ".trr-release-variant"
      - name: sign-transition-readiness-executables
        variant: ".trr-release-variant"
    commands:
      - func: "perform transition readiness report release"

  - name: release
    git_tag_only: true
    depends_on:
      - name: compile
        variant: "*"
    commands:
      - func: "perform release"

  - name: criterion
    commands:
      - func: "install rust toolchain"
      - func: "run criterion benchmarks"
      - func: "run criterion profile"
      - func: "upload criterion artifacts"

  - name: tpch-0.001-normalized-atlas-sql
    depends_on:
      - name: tpch-0.001-normalized-validation
    allow_for_git_tag: false
    disable: false
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-normalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/normalized
          workload_filename: "workload_norm_sf0.001.yml"
          s3_prefix: "SF0.001"

  - name: tpch-0.001-denormalized-atlas-sql
    depends_on:
      - name: tpch-0.001-denormalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-denormalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/denormalized
          workload_filename: "workload_denorm_sf0.001.yml"
          s3_prefix: "SF0.001"

  - name: tpch-0.01-normalized-atlas-sql
    depends_on:
      - name: tpch-0.001-normalized-validation
    allow_for_git_tag: false
    disable: false
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.01-normalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/normalized
          workload_filename: "workload_norm_sf0.01.yml"
          s3_prefix: "SF0.01"

  - name: tpch-0.01-denormalized-atlas-sql
    depends_on:
      - name: tpch-0.001-denormalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.01-denormalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/denormalized
          workload_filename: "workload_denorm_sf0.01.yml"
          s3_prefix: "SF0.01"

  - name: tpch-0.1-normalized-atlas-sql
    depends_on:
      - name: tpch-0.001-normalized-validation
    allow_for_git_tag: false
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.1-normalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/normalized
          workload_filename: "workload_norm_sf0.1.yml"
          s3_prefix: "SF0.1"

  - name: tpch-0.1-denormalized-atlas-sql
    depends_on:
      - name: tpch-0.001-denormalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.1-denormalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/normalized
          workload_filename: "workload_norm_sf0.1.yml"
          s3_prefix: "SF0.1"

  - name: tpch-1-normalized-atlas-sql
    depends_on:
      - name: tpch-0.001-normalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-1-normalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/normalized
          workload_filename: "workload_norm_sf1.yml"
          s3_prefix: "SF1"

  - name: tpch-1-denormalized-atlas-sql
    depends_on:
      - name: tpch-0.001-denormalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-1-denormalized-indexed.archive.gz"
          pipeline_dir: atlas_sql/denormalized
          workload_filename: "workload_denorm_sf1.yml"
          s3_prefix: "SF1"

  - name: tpch-0.001-normalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-normalized-indexed.archive.gz"
          pipeline_dir: bic/normalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.001"

  - name: tpch-0.001-denormalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-denormalized-indexed.archive.gz"
          pipeline_dir: bic/denormalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.001"

  - name: tpch-0.01-normalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.01-normalized-indexed.archive.gz"
          pipeline_dir: bic/normalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.01"

  - name: tpch-0.01-denormalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.01-denormalized-indexed.archive.gz"
          pipeline_dir: bic/denormalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.01"

  - name: tpch-0.1-normalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.1-normalized-indexed.archive.gz"
          pipeline_dir: bic/normalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.1"

  - name: tpch-0.1-denormalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.1-denormalized-indexed.archive.gz"
          pipeline_dir: bic/denormalized
          workload_filename: "workload.yml"
          s3_prefix: "SF0.1"

  - name: tpch-1-normalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-1-normalized-indexed.archive.gz"
          pipeline_dir: bic/normalized
          workload_filename: "workload.yml"
          s3_prefix: "SF1"

  - name: tpch-1-denormalized-bic
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run TPC-H benchmark"
        vars:
          dataset_filename: "tpch-1-denormalized-indexed.archive.gz"
          pipeline_dir: bic/denormalized
          workload_filename: "workload.yml"
          s3_prefix: "SF1"

  - name: tpch-0.001-normalized-validation
    allow_for_git_tag: false
    disable: false
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "validate TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-normalized-indexed.archive.gz"
          pipeline_dir: validation/normalized
          workload_filename: "workload_atlas_sql.yml"

  - name: tpch-0.001-denormalized-validation
    allow_for_git_tag: false
    disable: true
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 8.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "validate TPC-H benchmark"
        vars:
          dataset_filename: "tpch-0.001-denormalized-indexed.archive.gz"
          pipeline_dir: validation/denormalized
          workload_filename: "workload_atlas_sql.yml"

  - name: mem-usage-profiler-generator
    commands:
      - func: "generate memory usage tasks"

  - name: test-transition-readiness-report
    depends_on:
      - name: compile-trr
    commands:
      - func: "install rust toolchain"
      - func: "run transition readiness report test"

  - name: test-transition-readiness-report-integration
    depends_on:
      - name: compile-trr
    commands:
      - func: "bootstrap mongo-orchestration"
        vars:
          MONGODB_VERSION: 6.0
          TOPOLOGY: server
      - func: "install rust toolchain"
      - func: "run rust integration tests"
        vars:
          description: "run transition readiness report integration tests"
          cargo_test_flags: >-
            --features=integration
            --package=bic-trip
            --test log_parse_tests
            -- --test-threads=1 --show-output

  - name: sign-transition-readiness-executables
    allowed_requesters: ["github_tag", "ad_hoc"]
    depends_on:
      - name: compile-trr
    commands:
      - func: "sign trr windows"
        retry_on_failure: true
        variants: [trr-windows-64]
      - func: "sign trr linux"
        retry_on_failure: true
        variants: [trr-ubuntu2004]
      - func: "sign trr macos"
        retry_on_failure: true
        variants: [trr-macos, trr-macos-arm]

  - name: mongodb-schema-manager-sbom
    commands:
      - func: "install rust toolchain"
      - func: "generate SBOM"
        vars:
          project_folder: "mongodb-schema-manager"
      - func: "scan SBOM"
        vars:
          project_folder: "mongodb-schema-manager"

buildvariants:
  - name: static-analysis
    display_name: "* Static Analysis"
    run_on: [ubuntu1804-test]
    tasks:
      - name: clippy
      - name: rustfmt
      - name: unused-dependencies
      - name: gofmt
      - name: goimports
      - name: gomodtidy

  - name: ubuntu1804
    display_name: "Ubuntu 18.04"
    run_on: [ubuntu1804-large]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz

  - name: rhel76
    display_name: "RHEL 7.6"
    run_on: [rhel76-large]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz
      - name: test-spec-rewrite-and-type-constraints
      - name: test-go
      - name: .standalone !.7.0 !.8.0 !.rapid !.latest

  - name: ubuntu2204
    display_name: "MongoDB Integration Suite"
    tags: ["mongodb-suite-variant"]
    run_on: [ubuntu2204-large]
    tasks:
      - name: .standalone

  - name: macos
    display_name: "macOS"
    run_on: [macos-11]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz
      - name: test-go

  - name: macos-arm64
    display_name: "macOS arm64"
    run_on: [macos-13-arm64]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz
      - name: test-go

  - name: amazon2-arm64
    display_name: "Amazon2 arm64"
    run_on: [amazon2-arm64-large]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz
      - name: test-go

  - name: windows
    display_name: "Windows"
    run_on: [windows-64-vs2019-large]
    tasks:
      - name: compile
      - name: test-pipeline-generate
      - name: test-rust
      - name: test-rust-fuzz
      - name: test-go

  - name: benchmark
    display_name: "Benchmark"
    run_on: [ubuntu2004-medium]
    tasks:
      - name: criterion

  - name: tpch
    display_name: "TPC-H"
    cron: "0 0 * * 1,3,5" # M,W,F at 00:00
    run_on: [ubuntu2204-large]
    tasks:
      - name: tpch-0.001-normalized-atlas-sql
      - name: tpch-0.01-normalized-atlas-sql
      - name: tpch-0.001-normalized-validation

  - name: tpch-monthly
    display_name: "TPC-H Monthly"
    activate: false
    run_on: [ubuntu2204-large]
    tasks:
      - name: tpch-0.1-normalized-atlas-sql
      - name: tpch-0.001-normalized-validation

  - name: tpch-validation
    display_name: "TPC-H Validation"
    run_on: [ubuntu2204-large]
    tasks:
      - name: tpch-0.001-normalized-validation

  - name: mem-usage
    display_name: "Memory Usage Profiler"
    run_on: [amazon2-large]
    activate: true
    tasks:
      - name: mem-usage-profiler-generator

  - name: trr-integration-test
    display_name: "Transition Readiness Report Integration Test"
    run_on: [rhel76]
    tasks:
      - name: compile-trr
      - name: test-transition-readiness-report-integration

  - name: trr-macos
    tags: ["trr-release-variant"]
    display_name: "Transition Readiness Report - macOS"
    run_on: [macos-11]
    tasks:
      - name: compile-trr
      - name: test-transition-readiness-report
      - name: sign-transition-readiness-executables

  - name: trr-macos-arm
    tags: ["trr-release-variant"]
    display_name: "Transition Readiness Report - macOS arm64"
    run_on: [macos-13-arm64]
    tasks:
      - name: compile-trr
      - name: test-transition-readiness-report
      - name: sign-transition-readiness-executables

  - name: trr-windows-64
    tags: ["trr-release-variant"]
    display_name: "Transition Readiness Report - windows"
    run_on: [windows-64-vs2019-large]
    tasks:
      - name: compile-trr
      - name: test-transition-readiness-report
      - name: sign-transition-readiness-executables
        run_on: ubuntu2204-large

  - name: trr-ubuntu2004
    tags: ["trr-release-variant"]
    display_name: "Transition Readiness Report - linux"
    run_on: [ubuntu2004-large]
    tasks:
      - name: compile-trr
      - name: test-transition-readiness-report
      - name: sign-transition-readiness-executables

  - name: trr-release
    display_name: "Transition Readiness Report Release"
    run_on: [ubuntu2004-large]
    tasks:
      - name: trr-release

  - name: release
    display_name: "Release"
    run_on: [ubuntu1804-large]
    tasks:
      - name: release

  - name: mongodb-schema-manager-code-quality-security
    display_name: "MongoDB Schema Manager Code Quality and Security"
    run_on: [ubuntu2204-small]
    tasks:
      - name: mongodb-schema-manager-sbom
